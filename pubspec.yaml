name: ds_easy_db_firebase_realtime
description: "Firebase Realtime Database implementation for DS-EasyDB. Provides real-time data synchronization with streaming support."
version: 1.0.0
homepage: https://github.com/Dragon-InterActive/ds_easy_db_firebase_realtime
license: BSD-3-Clause

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.35.1'

dependencies:
  flutter:
    sdk: flutter
  ds_easy_db: ^1.0.0
  firebase_core: ^4.2.1
  firebase_database: ^12.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0